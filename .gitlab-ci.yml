image: maven:latest

stages:
  - build
  - test

cache:
  paths:
    - .m2/repository/
    - target/
tags:
    - dalfcs_gitlab_docker_ci
    
build:
  stage: build
  image: maven
  script:
    - mvn compile

test:
  stage: test
  script:
    - mvn test
